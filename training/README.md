# Project directory

This is the subproject containing code specific to the trainer, including the class holding your 'main()' function to run the trainer.

You will need to create a `src/` directory here.
